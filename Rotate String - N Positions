#include<stdio.h>
#include <stdlib.h>

int main()
{
    char a[1000];
    int n;
    scanf("%s%d",&a,&n);
    int b=strlen(a);
    int c=b-n;
    for(int i=c; a[i]!=NULL; i++)
    {
        printf("%c",a[i]);
    }
    for(int i=0; i<c; i++)
    {
        printf("%c",a[i]);
    }
    
}
